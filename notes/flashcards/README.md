# Flashcards

One markdown file per topic, `Q:` / `A:` pairs — convertible to Anki via any md→anki tool (e.g. `mdanki`).
Add cards **the day you learn the thing**, from your own notes, in your own words.

Suggested files as you progress: `math.md`, `classical-ml.md`, `deep-learning.md`, `transformers.md`, `rag.md`, `serving.md`, `fine-tuning.md`, `system-design.md`.
